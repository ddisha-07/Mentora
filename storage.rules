rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow authenticated users to read and upload assets
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
